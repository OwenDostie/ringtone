// required methods

// init (if some/all tables are missing)

// active_sessions
// server-side information abou t all currently connected clients 
    // session id, username, in lobby, date connected, last action & timestamp, 

// song files
    // song id, round id, game, username

// games 
// a history of all the games that have been played and the users